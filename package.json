{
  "name": "ucsb_class_scheduler",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ..",
    "deploy": "cp -a client/build/* public/",
    "postinstall": "npm run build && npm run deploy && echo 'client build finished'"
  },
  "dependencies": {}
}
